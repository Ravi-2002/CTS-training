it('renders Body section when AEMFlag is true', () => {
  // Arrange
  localStorage.setItem('AEMFlag', 'true');
  localStorage.setItem('viewLimitationMsg', '<strong>Test Msg</strong>');

  render(
    <MemoryRouter>
      <ViewLimitations />
    </MemoryRouter>
  );

  // Assert
  expect(screen.getByText('Test Msg')).toBeInTheDocument();
});