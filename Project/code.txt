import { Link } from "react-router-dom";

// Inside your services.map loop
<div>
  <p className="mb-1 fw-medium">{serviceName}</p>
  <small className="text-muted">{shortDescription}</small>
  <br />
  <Link to={`/service-details/${encodeURIComponent(serviceName)}`}>
    Tell me more
  </Link>
</div>


import React from "react";
import { useParams, useNavigate } from "react-router-dom";
import { useSelector } from "react-redux";

const ServiceDetails = () => {
  const { serviceName } = useParams();
  const navigate = useNavigate();
  const serviceDetailsMap = useSelector((state) => state.Home.services);

  let longDescription = "Description not found.";
  for (const category in serviceDetailsMap) {
    const service = serviceDetailsMap[category].find(
      (svc) => svc.serviceName === serviceName
    );
    if (service) {
      longDescription = service.longDescription || "No additional details available.";
      break;
    }
  }

  return (
    <div className="container mt-4">
      <h4 className="mb-3">{serviceName}</h4>
      <p>{longDescription}</p>
      <button className="btn btn-secondary" onClick={() => navigate(-1)}>
        Done
      </button>
    </div>
  );
};

export default ServiceDetails;


import { BrowserRouter as Router, Routes, Route } from "react-router-dom";
import BlockServices from "./BlockServices";
import ServiceDetails from "./ServiceDetails"; // we'll create this

function App() {
  return (
    <Router>
      <Routes>
        <Route path="/" element={<BlockServices encryptedMtn="your-value" />} />
        <Route path="/service-details/:serviceName" element={<ServiceDetails />} />
      </Routes>
    </Router>
  );
}