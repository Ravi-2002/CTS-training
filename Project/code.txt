test('renders modal with service details', () => {
  const store = mockStore({
    Detail: {
      serviceCalls: {
        services: {
          'Service 1': {
            isExpand: true,
            data: [
              {
                sfoCode: '123',
                serviceName: 'Test Service',
                shortDescription: 'Short Desc',
                longDescription: 'Long Desc',
                sfoEnabled: 'Y',
              },
            ],
          },
        },
        serviceStatus: { responseInfo: { responseCode: 0 } },
        getServices: true,
        failServices: null,
        serviceFail: false,
        confirmServices: null,
        serviceErrorFlag: false,
        confirmServiceFlag: false,
        servicesError: null,
        isFetching: false,
      },
    },
  });

  render(
    <Provider store={store}>
      <BlockServices selectedAccordian="BS" />
    </Provider>
  );

  const viewMoreButton = screen.getByText(/View More Details/i);
  fireEvent.click(viewMoreButton);
  expect(screen.getByText(/Further details/i)).toBeInTheDocument();
});