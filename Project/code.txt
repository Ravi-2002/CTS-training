import React from 'react';
import { render } from '@testing-library/react';
import { Provider } from 'react-redux';
import configureStore from 'redux-mock-store';
import { MemoryRouter } from 'react-router-dom';
import BlockCallsEmpty from '../BlockCallsEmpty'; // adjust path

const mockStore = configureStore([]);

describe('BlockCallsEmpty', () => {
  it('handles Content as null (else path taken)', () => {
    const store = mockStore({
      Detail: {
        blockedNumbers: [],
        addBlockCallMsg: {},
        isFetching: false,
        callsMessages: {
          aemResponse: null, // Content is null
        },
      },
    });

    render(
      <Provider store={store}>
        <MemoryRouter>
          <BlockCallsEmpty />
        </MemoryRouter>
      </Provider>
    );

    // you can assert here if needed, but this mainly covers the else path
  });
});